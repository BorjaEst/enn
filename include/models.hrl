%%%-------------------------------------------------------------------
%%% @author borja
%%% @doc
%%%
%%% @end
%%%-------------------------------------------------------------------

-define(sequential(Layers), model:sequential(Layers)).
-define(recurrent(Layers, Rlevel), model:recurrent(Layers, RLevel)).

